id: unit-12-files
kind: unit
blueprint_version: 1
lessons: 3
provenance: original
concepts:
  introduces: [file-read, file-write, with-statement]
  requires: [def-function, parameters, return-value, for-loop, list-append, if-statement, comparison,
             arithmetic, variable, print, elif-else, string-concat, float-type, list-sort,
             error-messages, list-index, break-statement, nested-loops, range-function,
             string-literal]
  practices: [transform-each, linear-search, list-literal, builtin-functions, running-total,
              count-by-condition, find-extreme, accumulator, int-type, f-string, comment, naming,
              input, type-conversion, string-methods, list-loop]
