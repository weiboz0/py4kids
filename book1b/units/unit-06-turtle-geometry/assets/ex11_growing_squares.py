# Student task: replace the small placeholder with the drawing in Exercise 11.
import turtle

side = 20
drawn = 0

# Draw a square at each current side length, then grow side and count the square.
for corner in range(4):
    turtle.forward(10)
    turtle.left(90)

turtle.done()
