id: unit-03-complexity
kind: unit
blueprint_version: 1
lessons: 1
provenance: original
concepts:
  introduces: [complexity]
  requires: [for-loop, nested-loops, list-loop, list-literal, range-function]
  practices: [str-split, input-parse, def-function, parameters, return-value, if-statement,
              elif-else, comparison, boolean, logical-ops, arithmetic, type-conversion,
              accumulator, list-append, list-sort, builtin-functions, dict-literal, dict-access,
              in-operator, while-loop, break-statement, string-literal, string-concat, print]
