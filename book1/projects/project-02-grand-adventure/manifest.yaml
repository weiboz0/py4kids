id: project-02-grand-adventure
kind: project
blueprint_version: 1
lessons: 4
provenance: original
concepts:
  introduces: []
  requires: [class-def, init-method, attributes, methods, file-read, file-write,
             dict-literal, list-literal, def-function]
  practices: [run-program, print, comment, input, string-literal, f-string,
              string-concat, string-methods, in-operator, variable, naming, int-type,
              arithmetic, type-conversion, boolean, comparison, if-statement, elif-else,
              conditional-nesting, while-loop, break-statement, for-loop, loop-counter,
              accumulator, import-statement, random-module, def-function, parameters,
              return-value, scope, list-literal, list-index, list-append, list-loop,
              builtin-functions, dict-literal, dict-access, file-read, file-write,
              with-statement, class-def, init-method, attributes, methods]
