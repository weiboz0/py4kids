# Secrets — NEVER commit (the repo is PUBLIC)
.gh-token
.env
.env.*
*.pem
*.key

# Student data — NEVER commit (the repo is PUBLIC)
student-data/
rosters/
grades/

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/

# Build artifacts
build/
*.pdf

# unit-09 file-I/O runtime scratch (created by lesson/solution notebooks at run time; never commit)
book1/units/unit-09-save-point/savegame.txt
book1/units/unit-09-save-point/settings.txt
book1/units/unit-09-save-point/overwrite_scores_tmp.txt

# checkpoint-04 file-I/O runtime scratch (written by Q3, read by Q4 at run time; never commit)
book1/checkpoints/checkpoint-04-year-one-finale/finale.txt

# project-02 capstone file-I/O runtime scratch (written then read by the save/load milestone; never commit)
book1/projects/project-02-grand-adventure/adventure_save.txt
book1/units/unit-09-save-point/dragon_slot.txt

# Local agent session state
.claude/

# book1b U12/U13/cp05 file-I/O runtime scratch (written by lesson/exercise/solution/checkpoint
# notebooks at run time via self-contained setup; never commit)
book1b/units/unit-12-files/*.txt
book1b/units/unit-13-objects/*.txt
book1b/checkpoints/checkpoint-05-files-and-objects/*.txt
book1b/projects/project-01-algorithm-challenge/*.txt
