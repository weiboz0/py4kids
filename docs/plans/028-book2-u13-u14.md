# Plan 028 — Book 2 U13 "Grids, Graphs & Traversal" + U14 "Two Pointers & Sliding Window"

> **For agentic workers:** run the full usaaio lifecycle (plan-review gate → phase implementation →
> ci-local → content-review gate → PR → pre-merge-guard → squash-merge). Steps use checkbox syntax.

**Goal:** Ship the final two Term-4 content units — `unit-13-grids-graphs` (introduces the TECHNIQUES
`graph-repr`, `bfs`, `dfs`, `flood-fill`) and `unit-14-two-pointers` (introduces the TECHNIQUE
`two-pointers`) — on the `solve(data:str)->str` contract, following the established Book-2 unit pattern.
These are the last new concepts before the capstone (`project-03`).

**Architecture:** Two units, each the standard Book-2 set (lesson/exercises/solutions/teacher-notes/
manifest). U13 teaches 2D-grid neighbour/bounds logic, **explicit graphs as adjacency-list dicts** (NO
class), **flood-fill** / connected components, **BFS** shortest-steps (a `deque` used as a FIFO queue —
`append` to enqueue, `popleft` to dequeue), and **DFS** reachability (recursion from U09, or a `deque`
stack). U14 teaches the **two-pointer** pattern: converging pointers on a sorted list and the sliding-window
sum/count. All five are reviewer-enforced TECHNIQUES (no new AST-detected feature). Graded exercises REQUIRE
the technique. Wire each into ci-local via the existing Book-2 per-entry block.

**Tech Stack:** Jupyter notebooks (nbformat), `tools/` checks, `scripts/ci-local.sh`,
`py4kids-tools --book book2`; Python 3 stdlib only (`from collections import deque` for BFS/DFS-stack).

**Spec:** `docs/designs/001-book2-algorithms.md` §7 (Term 4) + §5 (graphs as adjacency-list dicts, NO class);
`book2/syllabus.md`; `book2/curriculum/coverage-map.yaml` (U13/U14 entries); `book2/curriculum/concepts.yaml`
(all five new ids are `kind: technique`); plans 018–027 (the shipped pattern + ALL closure/gate lessons,
especially: the O(n²) multi-concat trap, the `nonlocal`/`global` untaught-and-scanner-blind trap, `+=`
untaught, comprehension dropped, `.pop` never taught).

## Global Constraints

Copied from the design + registry + prior-gate lessons; every task's requirements include this section.
**This plan introduces the LAST new techniques — after U14 every concept is taught.**

- **Contract:** every reference solution is a pure `solve(data: str) -> str`; ZERO `input()` in any
  executable cell; the submission wrapper appears ONLY in a `no-exec`-tagged LESSON cell (units get the
  `kind=="unit"` cell-lint exemption) or teacher-notes; NEVER in solutions.ipynb. Deterministic. Student
  code cells are EMPTY (`''`). Every cell has an `id`. Every output is an INTEGER or plain string — never a
  float.
- **NEWLY LEGAL here (the units' teaching targets, all scanner-invisible TECHNIQUES):** `graph-repr`
  (adjacency-list dict, NO class), `bfs`, `dfs`, `flood-fill` (U13); `two-pointers` (U14). These reuse
  already-taught concepts: the FEATURES `deque` (U10 — BFS queue is `append`+`popleft`; a DFS stack is
  `appendleft`+`popleft`), `recursion` (U09 — recursive DFS/flood-fill), `set-literal` (U04 — a `visited`
  set via `{…}`/`set()` + `.add` + `in`); and the TECHNIQUE `grid-2d` (U01); plus `dict-literal`/
  `dict-access` (Book-1). **DFS/flood-fill
  accumulate through a `visited` set or a result list PASSED AS AN ARGUMENT and mutated (`.add`/`.append`) —
  NEVER via `nonlocal`/`global`.**
- **STILL BANNED until their unit (U13 content only):** in U13, `two-pointers` (converging pointers OR a
  sliding window) is U14 — not yet. (U14 content may freely use the U13 graph techniques.)
- **ALWAYS BANNED (course-wide — the full accumulated list):** **NO `nonlocal` / `global`** (taught nowhere
  in Book 1/2, invisible to concept-scan — grep for them; DFS passes state as an argument instead). **NO
  `.pop()`** (untaught — a stack/queue is a `deque` with `appendleft`/`append`/`popleft`; peek `stack[0]`).
  **NO `comprehension`** (dropped from Book 2 — AST-check `ListComp`/`SetComp`/`DictComp`/`GeneratorExp`).
  **NO augmented assignment `+=`/`-=`/… (use `x = x + …`).** NO `del`. NO list/string repetition `[x]*n`/
  `'0'*n` (build the visited grid / distance array with `while`/`append`). NO chained comparison `a<=b<c`
  (use `0 <= r and r < rows`). set-ops via `.add`/`.discard`/`-` only (NEVER `.remove`/`&|^`-on-sets/
  `.union`/`.intersection`/`.difference`). NO untaught methods (`.join`/`.splitlines`/`.index`/`.count`/
  `.find`/`.pop`/**`.setdefault`** — note `.get` with a default IS taught and allowed). **NO untaught stdlib
  — beyond `itertools`/`collections.Counter`/`math.*`, also NO `collections.defaultdict` (the adjacency dict
  is a plain `dict` built with `if u not in adj: adj[u] = []` then `adj[u].append(v)`), NO `heapq`, NO
  `queue`/`queue.Queue` (BFS uses the taught `deque`, never a priority queue), NO `functools`, and NO `sys`
  / `sys.setrecursionlimit` (bound the input so plain recursion fits, never raise the limit).** NO
  base/number shortcuts (`bin`/`hex`/`format`/`:b`/`0x`/3-arg `pow`/`divmod`). **NO bare-container truthiness
  in a loop/test (`while queue:` / `if stack:`) — it is untaught (plan-026 `[FIXED]`); use `while
  len(queue) > 0`.** Allowed builtins ONLY `{len,min,max,sorted,sum,abs,round}` — NO `all`/`any`/`enumerate`/
  `zip`/`reversed`/`map`/`filter`.
  **Output assembly:** `.join` banned → build multi-item output ONE concat per statement (`result = result +
  piece`; the multi-concat `result = result + a + " " + b` form is O(n²) — the 439 s U11-Ex6 trap — so NEVER
  use it). concept-scan `--book book2` clean.
- **Enforcement split:** concept-scan + ad-hoc `detect()` confirm no premature/unlisted FEATURE (the new
  concepts are techniques — scanner-invisible — so detect() should show only the LISTED features
  `deque`/`recursion`/`set-literal`/`tuple`/`str-split` etc., each in requires/practices). An **AST check**
  for comprehensions, augmented-assignment (`ast.AugAssign`), list/str-repetition (`ast.BinOp` Mult with a
  List/Str operand), chained comparison (`ast.Compare` len(ops)>1); plus a **grep (code cells only)** for
  `\bnonlocal\b`, `\bglobal\b`, `\.pop\(`, `\.remove\(`, `\.setdefault\(`, `\bdel\b`, `\.join\(`,
  `\.union\(`, `\.intersection\(`, `\.difference\(`, `itertools`, `Counter`, `\bdefaultdict\b`, `\bheapq\b`,
  `\bimport queue\b`/`queue\.Queue` (NOT a bare `\bqueue\b` — a BFS `deque` may legitimately be named
  `queue`), `\bfunctools\b`, `\bimport sys\b`/`\bsys\.`, `setrecursionlimit`, `\bmath\b`, a bare `while \w+:`
  / `if \w+:`
  container-truthiness loop/test, and the banned builtins. The TECHNIQUES
  (`graph-repr`/`bfs`/`dfs`/`flood-fill`/`two-pointers`) are enforced by an APPROACH audit (adjacency dict,
  NO class; BFS uses a deque FIFO queue with `while len(queue) > 0`; DFS recursion/stack with a passed-in
  visited, never `nonlocal`; two-pointers / sliding window genuinely advances two indices and updates the
  running sum INCREMENTALLY — not a nested-loop brute force, and not a `prefix-sum` shortcut that skips
  pointer advancement).
- **Two-tier `practices` (plan-020 formula):** `practices = (detected ∪ manually-listed used) − requires −
  introduces − wrapper-artifacts (file-read, import-statement)`. `requires ∩ practices = ∅`,
  `practices ∩ introduces = ∅`; manifest == coverage-map exactly (incl. list order). Derive from ad-hoc
  `detect()`; hand-add scanner-invisible used concepts (techniques, `dict-access`, `input-parse`).
- **Rich, laddered problem sets:** ≥ 8 `## Exercise N` per unit (tooling counts `^## Exercise \d+`), each
  with Sample Input / Sample Output / Constraints; ≥ 2 cell-tagged `stretch`. Non-vacuous asserts killing
  each technique's signature mutant — BFS: the shortest-distance value (a wrong FIFO order / revisiting
  inflates it), an unreachable target (→ the stated "impossible" sentinel), a decisive-last cell;
  flood-fill: a single region, two disjoint regions, a diagonal NON-connection (4-neighbour only); DFS
  reachability: a reachable and an unreachable node, a cycle (visited prevents infinite recursion);
  graph-repr: a degree/neighbour query where an undirected edge must appear on BOTH endpoints; two-pointers:
  the converging-pointer pair that is decided at the LAST step, an empty/sentinel window, a window whose
  answer sits at the right boundary, **and a case that kills a "never-shrink" mutant (a window that must
  shrink from the left to stay valid — if the solver only grows, the answer is wrong)**. Decisive values
  last. **Bound grid/graph sizes so recursive DFS/flood-fill depth is safe: recursive-grid exercises cap at
  R·C ≤ 400 (e.g. ≤ 20×20), since a serpentine single region recurses ~R·C deep and Python's default limit
  is 1000** (deeper → `RecursionError`).
- **teacher-notes:** exactly the five heading strings `## Goals`/`## Pacing`/`## Common mistakes`/
  `## Discussion prompts`/`## Differentiation`; a per-exercise Big-O line; a concept → core-exercise matrix
  (≥1 non-stretch exercise per introduced technique); a pacing plan covering all allocated lessons
  (U13 = 3, U14 = 2).

## Out of scope

- No new unit/feature beyond the five listed techniques; no tooling change (they are reviewer-enforced
  techniques; per-entry ci-local auto-covers new dirs). If a scanner gap is found, enforce by review — do
  NOT add detectors in this plan.
- No checkpoint or capstone here (the capstone `project-03` is plan 029). **Note for plan 029:** two-pointers
  (U14) is the last pre-capstone entry with no later unit to practice it; if practice-completeness fires at
  capstone-time, plan 029 must add a CP4 after U14 or ensure the capstone practices it.
- **Verification is NOT out of scope:** Phase C is the named verification phase (blind-solve + mutation +
  full ci-local + 4-way content gate).

---

## Phases

### Phase A — U13 "Grids, Graphs & Traversal" (3 lessons)

**Files:** Create `book2/units/unit-13-grids-graphs/{lesson,exercises,solutions}.ipynb`, `teacher-notes.md`,
`manifest.yaml`; Modify `book2/curriculum/coverage-map.yaml` (U13 entry).

**Interfaces:** Consumes U01 `grid-2d`, U04 `set-literal`, U09 `recursion`, U10 `deque`, Book-1 dict/list.
Produces techniques `graph-repr`, `bfs`, `dfs`, `flood-fill` (prereqs for the capstone).

- [ ] **A1 — lesson.ipynb.** Hook (e.g. "fewest steps through a maze" / "how many rooms are connected?").
  Teach: (1) a 2D grid — neighbours (up/down/left/right), bounds checks (`0 <= r and r < rows`, no chained
  comparison); (2) an explicit graph as an **adjacency-list dict** `{node: [neighbours]}` built with
  `dict`/`list-append` (NO class, NO comprehension), degree = `len(adj[node])`, undirected edge added to
  BOTH endpoints; (3) **flood-fill** — recursively (or with a stack) fill a region, marking a `visited` set;
  (4) **BFS** shortest-steps — a `deque` FIFO queue (`append` enqueue, `popleft` dequeue), a `visited` set,
  a distance dict/array; (5) **DFS** reachability — recursion with a `visited` set **passed as an argument**
  (never `nonlocal`). Runnable `solve(data)` cells (bounded sizes); one `no-exec` wrapper cell.
- [ ] **A2 — exercises.ipynb.** ≥ 8 `## Exercise N` `solve(data)` problems REQUIRING the techniques, ≥1
  core per introduced technique (graph-repr, bfs, dfs, flood-fill): maze/grid shortest-steps (BFS),
  count-regions / largest-region (flood-fill, 4-neighbour), reachability / connected? (DFS), a
  degree/neighbour query on an adjacency-list graph (graph-repr), grid "can escape?" (BFS/DFS). ≥ 2
  `stretch`. EMPTY student cells; Sample I/O + Constraints (**recursive-grid exercises cap at R·C ≤ 400 so
  recursion is safe**; state the unreachable/"impossible" sentinel); integer/string outputs; the signature
  edges per Global Constraints; decisive value last.
- [ ] **A3 — solutions.ipynb (FRESH author, blind).** Mirror `## Exercise N`; pure `solve(data)`; adjacency
  dict (NO class); BFS with `deque` `append`/`popleft`; DFS recursion (or `deque` stack `appendleft`/
  `popleft`) with a `visited` set passed as an argument; flood-fill marking a visited set/grid built by
  `while`/`append`. NONE of `nonlocal`/`global`/`.pop`/`del`/`.join`/comprehension/`+=`/list-or-str-rep/
  chained-comparison/banned-builtins. Bounded depth. Non-vacuous asserts per the signature list.
- [ ] **A4 — teacher-notes.md.** Five exact headings; per-exercise Big-O (BFS/DFS O(V+E); flood-fill O(cells);
  grid O(R·C)); concept → core-exercise matrix; 3-lesson pacing with the CORE exercises assigned per lesson
  (L1 grids + adjacency graphs → the graph-repr core exercise; L2 flood-fill + BFS → the flood-fill and BFS
  core exercises; L3 DFS + harder → the DFS core exercise — so nothing is practiced before its lesson and no
  single lesson is overloaded); stretches stay optional homework; common mistakes (forgetting `visited` →
  infinite loop / RecursionError; BFS using a LIFO stack instead of a FIFO queue gives wrong distances;
  diagonal vs 4-neighbour; mutating the grid while iterating; reaching for `.pop`/`nonlocal`/`defaultdict`).
- [ ] **A5 — manifest.yaml + map.** `introduces: [graph-repr, bfs, dfs, flood-fill]`; `requires:` trimmed to
  genuinely-used; `practices:` per the formula; disjointness; manifest == map (incl. order).
- [ ] **A6 — verify U13 in isolation:** all `--book book2` checks PASS; concept-scan clean; ad-hoc `detect()`
  shows only LISTED features (no premature/unlisted); AST-check (no comprehension/`+=`/list-or-str-rep/
  chained-comparison); **grep (code cells) for `\bnonlocal\b`, `\bglobal\b`, `\.pop\(`, `\.remove\(`,
  `\bdel\b`, `\.join\(`, banned builtins** — all absent; approach audit (adjacency dict NO class, BFS deque
  FIFO, DFS visited-as-argument, 4-neighbour); each technique has a core exercise.

### Phase B — U14 "Two Pointers & Sliding Window" (2 lessons)

**Files:** Create `book2/units/unit-14-two-pointers/{lesson,exercises,solutions}.ipynb`, `teacher-notes.md`,
`manifest.yaml`; Modify `book2/curriculum/coverage-map.yaml` (U14 entry).

**Interfaces:** Consumes Book-1 (list-index/while/accumulator/comparison) + U04 `sorted-key`. Produces
technique `two-pointers`.

- [ ] **B1 — lesson.ipynb.** Hook (e.g. "find two prices that sum to the budget" / "longest stretch under a
  limit"). Teach: (1) converging two pointers on a SORTED list — `lo`/`hi` moving inward by comparison
  (pair-sum, closest pair); (2) the sliding window — `left`/`right` indices bounding a contiguous run, with a
  running sum/count updated as the window grows/shrinks (longest/shortest window meeting a condition). Stress
  WHY it is linear vs the nested-loop brute force. Runnable `solve(data)` cells; one `no-exec` wrapper cell.
- [ ] **B2 — exercises.ipynb.** ≥ 8 `## Exercise N` REQUIRING two-pointers/sliding-window: pair summing to
  a target on a sorted list, count pairs under a threshold, longest window with sum ≤ K, shortest window
  summing ≥ K, a converging-pointer decision. ≥ 2 `stretch`. **EVERY window-SUM problem MUST constrain the
  list values to be NON-NEGATIVE** — the linear grow/shrink window is only valid for non-negative values
  (with negatives the monotonicity breaks and the two-pointer answer is wrong; state `0 <= value` in the
  constraints). At least one core problem sorts by a SECONDARY field so `sorted-key` is genuinely used (else
  B5 moves `sorted-key` to `practices`). EMPTY student cells; Sample I/O + Constraints; the signature edges
  (decided-at-last-step, empty/boundary window, answer at the right boundary, a must-shrink window);
  decisive value last.
- [ ] **B3 — solutions.ipynb (FRESH author, blind).** Mirror `## Exercise N`; pure `solve(data)`; genuine
  two-pointer / sliding-window index advancement (NOT a disguised nested-loop brute force); `sorted(key=…)`
  where a sort is needed (no lambda); NONE of `nonlocal`/`.pop`/`del`/`.join`/comprehension/`+=`/
  list-or-str-rep/chained-comparison/banned-builtins. Non-vacuous asserts per the signature list.
- [ ] **B4 — teacher-notes.md.** Five exact headings; per-exercise Big-O (O(n) window / O(n log n) if a sort
  dominates); concept → core-exercise matrix; 2-lesson pacing (L1 converging pointers; L2 sliding window);
  common mistakes (advancing the wrong pointer; off-by-one on the window bounds; forgetting to shrink the
  window; re-summing the window each step instead of updating incrementally → O(n²)).
- [ ] **B5 — manifest.yaml + map.** `introduces: [two-pointers]`; `requires:` trimmed to genuinely-used —
  keep `sorted-key` ONLY if ≥1 core exercise's reference solution uses a keyed sort (`sorted(key=…)` by a
  secondary field); if no exercise needs a key, move `sorted-key` to `practices` (plain value-sorting is
  `list-sort`, not `sorted-key`); `practices:` per the formula; disjointness; manifest == map.
- [ ] **B6 — verify U14 in isolation:** all `--book book2` checks PASS; concept-scan clean; AST + grep
  (including `\bnonlocal\b`) as in A6; approach audit (genuine two-pointer/window advancement, not a nested
  loop, **and an INCREMENTAL running-sum updated as the window moves — NOT a `prefix-sum` shortcut** that
  would solve the window problem without advancing the pointers); each technique has a core exercise.

### Phase C — Verification (named verification phase)

- [ ] **C1 — Blind-solve + mutation pass** for BOTH units: solve each exercise from the student-facing
  materials; run each `solve()` on its stated Sample I/O and confirm a match; confirm the signature mutants
  die (BFS-wrong-order/no-visited, flood-fill diagonal, DFS no-visited-cycle, graph both-endpoints,
  two-pointer advance-wrong/off-by-one, **and the never-shrink window mutant — a window that only grows and
  never shrinks from the left must fail an assert**). Fix gaps by adding crafted asserts (pristine-pass + mutant-kill),
  never by changing a correct solver. Check recursion depth is bounded by the stated constraints.
- [ ] **C2 — Full `scripts/ci-local.sh`** (orphan-kill first; foreground `TMPDIR=/dev/shm … > SCRATCHPAD/log`):
  ALL GREEN, pre-merge-guard OK; all 10 `--book book2` per-entry checks PASS for U13 + U14.
- [ ] **C3 — 4-way content-review gate** (`docs/content-review-gate.md`): `[self]`/`[sol]`/`[glm]`/`[fable]`,
  all solve blind. Iterate fix → re-review to consensus (all four APPROVE / APPROVE WITH NITS, every `[OPEN]`
  resolved). (Note: `opencode`/[glm] may return a placeholder then re-notify async; re-dispatch on a
  non-verdict.)
- [ ] **C4 — Post-execution report** in this plan; PR via `gh`; `pre-merge-guard --pr`; squash-merge.

## Post-Execution Report

_(filled at Phase C)_

## Plan Review

_(4-way plan-review gate — consensus before any implementation)_

### Round 1 (2026-09-09, HEAD 01584b1)
- **[self] — APPROVE.** Mirrors the shipped unit-plan pattern; introduces/requires match the coverage-map;
  all five new concepts are `kind: technique` (scanner-invisible, reviewer-enforced), reusing the taught
  features deque/recursion/set-literal/grid-2d. Closure correct: graph techniques legal at U13, two-pointers
  at U14; within-U13 no two-pointers; the CP3 `nonlocal` lesson is applied (DFS passes `visited` as an
  argument; `nonlocal`/`global` explicitly banned + grepped). BFS pinned as a FIFO `deque` (append+popleft),
  DFS as visited-as-argument. Verification phase (C) present; CP4 carry-forward noted for plan 029.
  **Self-flag to tighten in round-1 if reviewers concur:** the import ban names `itertools`/`Counter`/`math`
  but should also explicitly forbid the untaught graph shortcuts `collections.defaultdict`, `heapq`, and
  `queue.Queue` (the adjacency dict is a plain `dict` with `if k not in adj: adj[k] = []` + append; BFS uses
  the taught `deque`, never a priority queue). Awaiting [sol]/[glm]/[fable].
#### Round-1 verdicts — [self] APPROVE · [fable] APPROVE WITH NITS · [glm] APPROVE WITH NITS · [sol] REJECT

Conventions/metadata/closure-base/verification-phase all confirmed sound. Findings (all addressed at HEAD):
1. `[FIXED]` [sol Must / fable Must / self] untaught scanner-blind stdlib unbanned → banned + grepped
   `collections.defaultdict` (adjacency dict is a plain `dict` with `if u not in adj: adj[u] = []`), `heapq`,
   `queue`/`queue.Queue`, `functools`, `sys`/`setrecursionlimit`.
2. `[FIXED]` [sol Must / glm] U14 window-SUM exercises now REQUIRE non-negative values (`0 <= value`) — the
   linear grow/shrink is invalid with negatives; B6 audit requires an incremental running-sum, not a
   `prefix-sum` shortcut.
3. `[FIXED]` [sol Must] a "never-shrink" window mutant added to the assert/mutation guidance (Global + C1).
4. `[FIXED]` [fable Should] bare-container truthiness banned (`while queue:` → `while len(queue) > 0`) +
   grepped + approach-audited.
5. `[FIXED]` [fable Should] `.setdefault` banned + grepped (note: `.get` with a default IS taught — kept);
   adjacency built with an `if u not in adj` pre-check.
6. `[FIXED]` [fable Nice / sol Nice] recursion-depth cap made NUMERIC: recursive-grid exercises cap at
   R·C ≤ 400 (serpentine flood-fill recurses ~R·C deep; default limit 1000).
7. `[FIXED]` [sol Nice / glm] `grid-2d` reworded as a TECHNIQUE (deque/recursion/set-literal are the reused
   FEATURES).
8. `[FIXED]` [glm] set-ops grep extended to `.union(`/`.intersection(`/`.difference(`.
9. `[FIXED]` [glm] A4 pins per-lesson CORE-exercise assignments (DFS core in L3; stretches optional).
10. `[FIXED]` [glm] B5 keeps `sorted-key` in requires only if a keyed-sort exercise exists, else → practices.

- **[self] Round 2 — APPROVE** (revision addresses every Must/Should/Nice).

#### Round-2 verdicts — [self] APPROVE · [glm] APPROVE · [fable] APPROVE WITH NITS · [sol] REJECT (3 wording)
(Round-2 reviewers were re-dispatched after a session rate-limit killed the first attempt.) [glm] APPROVE
(all nits resolved, new-defect scan clean). [fable] APPROVE WITH NITS (all 5 resolved; one new nit = the
`queue`-grep self-collision). [sol] REJECT on three cross-reference/wording tightenings — all `[FIXED]`:
- `[FIXED]` [sol#3 = fable] `\bqueue\b` grep self-collides with the mandated `while len(queue) > 0` → narrowed
  to `\bimport queue\b`/`queue\.Queue` (and `\bsys\b` → `\bimport sys\b`/`\bsys\.`).
- `[FIXED]` [sol#1] B6 didn't restate the incremental-running-sum / no-`prefix-sum`-shortcut requirement →
  added to the B6 approach audit.
- `[FIXED]` [sol#2] C1's mutant list didn't name the never-shrink window mutant → added.

- **[sol] Round 3 — APPROVE.** All three wording items resolved; no blocking findings.

#### CONSENSUS — plan-review gate CLOSED
[self] APPROVE · [glm] APPROVE · [fable] APPROVE WITH NITS · [sol] APPROVE. All four APPROVE / APPROVE WITH
NITS, no open blocker. **Implementation authorized (Phase A).**

## Content Review

_(4-way content-review gate — findings `[OPEN]`/`[FIXED]`/`[WONTFIX]`, all resolve before merge)_

### Review 1 — [self] (2026-09-09)
- **Verdict**: APPROVE

Pre-gate verification: all 10 `--book book2` checks PASS; every U13 + U14 solution matches its stated Sample
I/O; all inline asserts pass (real exec). Three author defects were caught during Phase-C verification and
FIXED before the gate:
1. `[FIXED]` U14 Ex2 (Pairs Below the Alarm) — inline assert expected `3` but the rule is strict `sum < T`
   (`1 3 7 20`/T=10 → {1+3,1+7} = `2`); the solution is correct, the crafted assert was wrong → `2`.
2. `[FIXED]` U14 Ex9 (Minimum Rescue Boats) — both the STATEMENT sample output and the assert said `3`, but
   the minimum for `{3,3,4,5,7,8}`/L=10 is `4` (8 can't pair: 8+3=11>10, so it's solo; brute-force confirmed);
   the solution is correct → fixed the statement sample AND the assert to `4`.
3. `[FIXED]` U13 solutions — hand-rolled integer parsing via `"0123456789".index(c)` (untaught method →
   concept-scan FAIL) → replaced with the taught `int(c)`.
- **Closure**: U13 recursion/deque/set-literal legal (in requires); graph techniques introduced here; U14
  two-pointers introduced. AST+grep CLEAN — no `nonlocal`/`global`/`.pop`/`.join`/`.index`/`.setdefault`/
  comprehension/`+=`/list-or-str-repetition/chained-comparison/`defaultdict`/`heapq`/`queue`/bare-container-
  truthiness; BFS uses a deque FIFO with `while len(queue) > 0`; DFS/flood-fill pass `visited` as an argument
  (no `nonlocal`); U14 window-sum constrains non-negative values + incremental running-sum; one keyed
  `sorted(key=…)`.
- **Project-first**: U13 opens on "fewest steps through a maze / how many rooms connected"; U14 on "two
  prices summing to the budget / longest stretch under a limit".
- **Per-concept core practice**: U13 — graph-repr (Ex1,6,7), flood-fill (Ex2,3), bfs (Ex4), dfs (Ex5,6);
  U14 — two-pointers across Ex1–7. **Manifest==map**: U13 requires trimmed (dropped `for-loop`, all-while),
  set-ops→practices; U14 keeps `sorted-key` (keyed sort used); practices per formula, disjoint.

No open [self] findings. Awaiting [sol]/[glm]/[fable] + full `ci-local.sh`.

### Reviews 2–3 — [fable]/[sol] (2026-09-09); [glm] non-verdict (opencode flaked)

Full `ci-local.sh`: ALL GREEN, pre-merge-guard OK. [fable] APPROVE WITH NITS (2,500 randomized oracle
cross-checks; all 18 blind-solves match; all prior fixes re-verified). [sol] REJECT (3 Must + 1 Should; all
solutions correct + samples match, findings are assert-coverage/consistency). [glm] returned a placeholder
("review is running") — opencode flake; re-dispatched in Round 2.

Findings:
1. `[FIXED]` [sol Must] U13 Ex4 & Ex9 — a FIFO→LIFO traversal mutant survived every assert. → added
   distinguishing asserts: Ex4 `"4 4\nT.S.\n.#..\n....\n.###" == "2"` (FIFO 2, LIFO 8); Ex9
   `"5 5 1\n1 2\n2 3\n3 4\n4 5\n1 5" == "2"` (FIFO 2, LIFO 3) — both computed from the real solution and
   confirmed to kill a `.popleft`→`.pop` mutant.
2. `[FIXED]` [sol Must] U14 Ex9 — removing `sorted(weights)` survived every assert. → added
   `"3 10\n6 5 5" == "2"` (an UNSORTED input; the no-sort mutant returns 3).
3. `[FIXED]` [sol Should = fable#2] U13 teacher-notes mislabeled Ex5 as DFS (it is BFS reachability) and Ex7
   as component-count (it is connectivity). → concept matrix, pacing, and Big-O corrected; added a note that
   Ex5/6/7 are order-independent reachability/connectivity (the FIFO distinction is exercised by Ex4/Ex9).
4. `[WONTFIX]` [sol Must #3] `import sys` in the `no-exec` "Submit the Solver" wrapper — this is the universal
   contest-submission harness present in EVERY shipped unit (U01–U12) and confirmed by [fable] as identical to
   U10/U12; the `sys` ban targeted `sys.setrecursionlimit` and solution-logic imports, not the no-exec
   wrapper (which cell-lint/concept-scan already exempt as an import wrapper-artifact). Removing it would break
   the established, correct pattern.
5. `[FIXED]` [fable Low #1 = glm OPEN2] U13 solutions hand-rolled `read_number`/`number_text` digit-parse
   loops instead of the taught `int(...)`/`str(...)` idiom used in U14 and every prior Book-2 unit. → all 9
   solution cells rewritten to `int(...)`/`str(...)`, both helper defs removed; solutions re-executed clean
   (asserts unchanged, all hold). Raised by two reviewers, so simplified rather than WONTFIX'd — the reference
   students read now uses the taught idiom.
6. `[WONTFIX]` [fable Low #3] U13 `visited` set sentinels — valid taught construct; correctness/mutation
   unaffected. Left as-is (the visited-set pattern is exactly what the unit teaches).

### Review 4 — [glm] (2026-09-09; real verdict, after an opencode placeholder)

APPROVE WITH NITS. Blind-solved all non-trivial exercises; every answer matches samples + solution asserts
(U13 Ex4=8 with the DFS-first mutant returning 14 → the added assert kills wrong-order traversal; U13
Ex2=6/Ex3=3/Ex8=7/Ex9=5; U14 Ex2=6/Ex5=3/Ex6=3/Ex7=4/Ex9=4). Two nits, both already covered above:
OPEN1 = the Ex5 DFS→BFS mislabel (finding #3, `[FIXED]`); OPEN2 = the `read_number` idiom (finding #5,
`[FIXED]`). "No blocking correctness, closure, timing, or scanner findings."

### Consensus

[self] APPROVE · [fable] APPROVE WITH NITS · [glm] APPROVE WITH NITS · [sol] REJECT→all Musts `[FIXED]`.
Every `[OPEN]`/Must/Should resolved (fixed or documented WONTFIX). Re-verifying with a full `ci-local.sh` on
the final content, then a round-2 [sol]/[fable] re-confirm before PR.
