# Plan 022 — Book 2 U06 "Greedy" + U07 "Simulation & Ad Hoc"

**Goal:** Ship the first two Term-2 units — `unit-06-greedy` (introduces the TECHNIQUE `greedy`) and
`unit-07-simulation` (introduces the TECHNIQUE `simulation`) — on the `solve(data:str)->str` contract,
following the established Book-2 technique-unit pattern (U02/U03/U05).

**Architecture:** Two units, each the standard Book-2 set (lesson/exercises/solutions/teacher-notes/
manifest). Both introduced concepts are reviewer-enforced **techniques** (scanner-invisible in both
directions), taught in the lesson + teacher-notes and reinforced by the exercises. U06 greedy =
sort-then-sweep / locally-best choice; U07 simulation = follow-the-rules step-by-step state (including on a
grid). Graded exercises are `solve(data)` problems that REQUIRE the technique (a correct greedy choice; a
faithful simulation). Wire each into ci-local via the existing Book-2 per-entry block (auto-covers new
unit dirs).

**Tech Stack:** Jupyter notebooks (nbformat), `tools/` checks, `scripts/ci-local.sh`,
`py4kids-tools --book book2`; Python 3 stdlib only.

**Spec:** `docs/designs/001-book2-algorithms.md` (§4 unit anatomy, §5 two-tier concepts, §7 U06/U07);
`book2/syllabus.md`; `book2/curriculum/coverage-map.yaml` (U06/U07 entries); `book2/curriculum/
concepts.yaml`; plans 018–021 (the shipped Book-2 unit pattern — all closure rules transfer); the
plan-019/020/021 content-gate lessons (mutation categories; `.remove` ban; untaught-builtin ban;
one-directional concept-scan; the corrected set-ops recipe).

## Global Constraints

Copied from the design + registry + prior-gate lessons; every task's requirements include this section.

- **Contract:** every reference solution is a pure `solve(data: str) -> str`; ZERO `input()` in any
  executable cell; the submission wrapper `import sys; print(solve(sys.stdin.read()))` appears ONLY in a
  `no-exec`-tagged LESSON cell (allowed for units — cell-lint exempts `no-exec` for `kind=="unit"`) or in
  teacher-notes; NEVER in solutions.ipynb (exec-solutions runs every cell). Deterministic. Student code
  cells are EMPTY (`''`).
- **New concepts are TECHNIQUES (scanner-invisible):** `greedy` (U06) and `simulation` (U07) are
  reviewer-enforced — taught genuinely in the lesson/teacher-notes and USED by the exercises; each is
  hand-listed nowhere as a practice of its own unit (it is that unit's `introduces`). A used technique from
  an EARLIER unit must be hand-listed in practices; a not-yet-introduced technique must NOT appear.
- **Closure (same scanner rules as U02–U05):** only Book-1 + U01–U0(N-1) concepts + this unit's introduce.
  If sets are used: set-ops ONLY via `.add`/`.discard` on a `{…}`/`set()` var in-cell, or `-`; **NOT
  `.remove`** (scanner silently credits it, but U04 taught only `.add`/`.discard`); NEVER `&|^`; NEVER
  `.union`/`.intersection`/`.difference`. `sorted(seq, key=named_fn)` (NO lambda). binary-search
  `lo/hi/mid` and fixed-depth nested-loop complete-search are allowed (taught U05). **Nothing not yet
  taught:** no `prefix-sum` (U08, a technique — reviewer-caught), no converging two-pointer (U14, a
  technique — reviewer-caught); and no not-yet-taught FEATURE — `recursion`/`backtracking` (U09), `deque`
  (U10), `comprehension`/generator expressions (U09), `bitwise-ops`/`base-conversion` (U11) — these ARE
  scanner-detectable, so concept-scan reds them as used-but-unlisted. NO untaught methods
  (.join/.splitlines/.index/.count/.find/.pop). Allowed builtins ONLY `{len,min,max,sorted,sum,abs,round}` —
  NO `all`/`any`/`enumerate`/`zip`/`reversed`/`map`/`filter`. **Enforcement split:** concept-scan +
  ad-hoc `detect()` catch premature FEATURES + untaught methods; the `.remove` ban and the untaught-builtin
  ban (`all`/`any`/`enumerate`/`zip`/`reversed`/`map`/`filter`) are scanner-BLIND (bare-name calls / a
  silently-credited method) and must be enforced by **grep + reviewer read**, not `detect()`. concept-scan
  `--book book2` clean.
- **Two-tier `practices` — the plan-020 formula:** `practices = (detected ∪ manually-listed used
  concepts) − requires − introduces − wrapper-artifacts (file-read, import-statement)`. A concept already in
  the unit's `requires` NEVER also appears in `practices` (so U06 keeps `sorted-key`/`tuple` in `requires`
  only; U07 keeps `input-parse`/`grid-2d` in `requires` only). What remains in `practices`: `str-split`
  (feature, if used and not in requires) + any earlier TECHNIQUE used and not in requires (`complexity`/
  `binary-search`/`complete-search`/`boolean-algebra`) + the Book-1 concepts used. `requires ∩ practices =
  ∅` and `practices ∩ introduces = ∅` (matching U01–U05); manifest == coverage-map exactly. Derive
  `str-split`/features from an ad-hoc `detect()` over the three notebooks; hand-add scanner-invisible used
  concepts (`dict-access` via `.get`/`[]`; techniques).
- **Rich, laddered problem sets:** ≥ 8 `## Exercise N` per unit (tooling counts `^## Exercise \d+`), each
  with Sample Input / Sample Output / Constraints; ≥ 2 cell-tagged `stretch` (heading "Challenge"); core
  content never depends on stretch. Non-vacuous asserts: sample + crafted edge cases killing off-by-one
  (drop-last / range(n-1)), wrong-aggregate/tie boundary, self-pair, and **decisive-last-AFTER-SORT** for
  any sort-then-scan/greedy solution. Decisive values placed last (and last-after-sort).
- **teacher-notes:** exactly the five heading strings `## Goals`/`## Pacing`/`## Common mistakes`/
  `## Discussion prompts`/`## Differentiation`; a per-exercise Big-O line; a pacing plan covering all
  allocated lessons (U06 = 2, U07 = 2).

## Out of scope

- No new feature or tooling change (technique units add no AST-detected concept; per-entry ci-local
  auto-covers new dirs).
- No checkpoint (CP2 is a later plan — it assesses U06–U08 and needs all three merged first).
- **Verification is NOT out of scope:** Phase C is the named verification phase (blind-solve + mutation +
  full ci-local + 4-way content gate).

---

## Phases

### Phase A — U06 "Greedy" (2 lessons)

**Files:**
- Create: `book2/units/unit-06-greedy/{lesson,exercises,solutions}.ipynb`, `teacher-notes.md`, `manifest.yaml`
- Modify: `book2/curriculum/coverage-map.yaml` (fill the U06 entry's `practices` + trim `requires` to used — both mirror the manifest since manifest==map)

**Interfaces:**
- Consumes: U04 `sorted-key`/`tuple` + Book-1 (for-loop, accumulator, comparison, list-sort).
- Produces: technique `greedy` (prereq for CP2 and later units).

- [ ] **A1 — lesson.ipynb.** Contest hook (e.g. "attend the most non-overlapping events" or "make change
  with fewest coins where greedy is valid"). Teach: the greedy pattern = sort by the right key, then sweep
  taking the locally-best choice; an informal "why it works" (exchange argument in plain words); and a
  caution that greedy is NOT always correct (one counter-example). Runnable `solve(data)` cells; one
  `no-exec`-tagged cell showing the wrapper.
- [ ] **A2 — exercises.ipynb.** ≥ 8 `## Exercise N`, each a `solve(data)` problem where a correct greedy
  choice (with the right sort key) solves it: interval scheduling / max non-overlapping, minimize-lateness,
  fewest-coins (valid denominations), assign-to-minimize, deadline ordering, max-items-under-budget. ≥ 2
  `stretch`. **Every output is an INTEGER or a plain string (`YES`/`NO`/a name/a count) ONLY — never a
  float and never a decimal/fractional string of any kind (no `%.2f`, no `"3.5"`, no ratio output).** No
  fractional-knapsack-style problems. (Token-compare asserts are float-fragile; keeping every answer an
  exact integer or word sidesteps it entirely.) EMPTY student cells; Sample I/O + Constraints; decisive
  value last / after sort.
- [ ] **A3 — solutions.ipynb (FRESH author, blind).** Mirror `## Exercise N`; pure `solve(data)`; each uses
  `sorted(seq, key=named_fn)` + a sweep. Non-vacuous asserts incl. tie at the sort boundary,
  decisive-last-after-sort, and a case where a naive non-greedy/wrong-key choice differs.
- [ ] **A4 — teacher-notes.md.** Five exact headings; per-exercise Big-O (dominated by the O(n log n) sort);
  2-lesson pacing (L1 the pattern + why-it-works; L2 harder greedy + a counter-example); common mistakes
  (wrong sort key; assuming greedy always works).
- [ ] **A5 — manifest.yaml + map.** `introduces: [greedy]`; `requires:` = the genuinely-used prereqs (the
  map seeds `sorted-key`/`list-sort`/`for-loop`/`accumulator`/`tuple`/`comparison` — keep those actually
  used; `sorted-key`/`tuple` stay HERE, not practices). `practices:` = `used − requires − introduces −
  wrapper-artifacts`: mechanically, every concept the U06 solutions use that is NOT already in U06's
  `requires` list above, NOT `greedy`, and NOT a wrapper artifact — each listed once. (This is `str-split`,
  the Book-1 concepts used, and `input-parse` — the parsing technique every `solve(data)` uses, which is
  legitimately a U06 practice because it is NOT in U06's `requires`; note this differs from U07, where
  `input-parse` IS a require.) `requires ∩ practices = ∅`, `practices ∩ introduces = ∅`; manifest == map.
- [ ] **A6 — verify U06 in isolation:** all `--book book2` checks PASS; concept-scan clean (an ad-hoc
  `detect()` catches a premature FEATURE such as `comprehension`/`recursion`/`deque`; a premature TECHNIQUE
  like `prefix-sum` is scanner-invisible → reviewer-confirmed). Separately **grep** the notebooks for
  `.remove(` and the banned builtins (`all`/`any`/`enumerate`/`zip`/`reversed`/`map`/`filter`) — these slip
  `detect()`. Confirm greedy is genuinely taught + used.

### Phase B — U07 "Simulation & Ad Hoc" (2 lessons)

**Files:**
- Create: `book2/units/unit-07-simulation/{lesson,exercises,solutions}.ipynb`, `teacher-notes.md`, `manifest.yaml`
- Modify: `book2/curriculum/coverage-map.yaml` (fill the U07 entry's `practices` + trim `requires` to used — both mirror the manifest since manifest==map)

**Interfaces:**
- Consumes: Book-1 (for/while loops, if, list, accumulator) + U01 `grid-2d`/`input-parse`.
- Produces: technique `simulation` (prereq for CP2 and later units).

- [ ] **B1 — lesson.ipynb.** Hook: "follow the rules exactly" (e.g. a robot on a grid, a queue of events,
  a game of steps). Teach: model the state, apply each rule/step in order, watch the edges (bounds,
  off-by-one, termination). Runnable `solve(data)` cells (incl. a grid walk using `grid-2d`); one
  `no-exec` wrapper cell.
- [ ] **B2 — exercises.ipynb.** ≥ 8 `## Exercise N` `solve(data)` problems that REQUIRE faithful
  step-by-step simulation: grid robot / bounded walk, tick-by-tick resource/state update, event processing
  by index-walk (NO `.pop`/`deque` — both banned), wrap-around counter (plain `%` arithmetic only — keep the
  framing away from the U11 `modular-arithmetic` technique: no modular identities/inverses), rule-driven
  transformation. ≥ 2 `stretch`. EMPTY student cells; Sample I/O + Constraints; decisive value last; a case
  exercising the boundary/termination edge.
- [ ] **B3 — solutions.ipynb (FRESH author, blind).** Mirror `## Exercise N`; pure `solve(data)`;
  fixed-depth loops / bounded while-loops (guaranteed to terminate); NO recursion. Non-vacuous asserts incl.
  the boundary/edge/termination case, off-by-one on the last step, and wrong-order-of-operations.
- [ ] **B4 — teacher-notes.md.** Five exact headings; per-exercise Big-O (typically O(steps) or O(R·C));
  2-lesson pacing (L1 state + one-step rules; L2 grid/queue simulation + edges); common mistakes
  (off-by-one on the final step; updating state in the wrong order; missing a boundary).
- [ ] **B5 — manifest.yaml + map.** `introduces: [simulation]`; `requires:` = genuinely-used prereqs (the
  map seeds `for-loop`/`while-loop`/`if-statement`/`list-literal`/`accumulator`/`grid-2d`/`input-parse` —
  `input-parse`/`grid-2d` stay HERE, not practices). `practices:` = `used − requires − introduces −
  wrapper-artifacts` = `str-split` (if used) + Book-1 concepts used; `requires ∩ practices = ∅`; manifest
  == map.
- [ ] **B6 — verify U07 in isolation** (same check list + grep as A6); concept-scan clean; confirm NO
  premature feature (ad-hoc `detect()`) and NO premature technique (reviewer), and that `simulation` is the
  only new technique.

### Phase C — Verification (named verification phase)

- [ ] **C1 — blind-solve + mutation self-check** on both units (off-by-one, wrong-aggregate/tie, self-pair,
  boundary, decisive-last-after-sort — every mutant fails an assert; every pristine solver passes). Confirm
  greedy/simulation genuinely taught + exercised; no premature technique. Fix any vacuous assert.
- [ ] **C2 — full `scripts/ci-local.sh` ALL GREEN both books.**
- [ ] **C3 — commit Phase A+B+C** on `feature/plan-022-book2-u06-u07`.
- [ ] **C4 — 4-way content-review gate** ([self]/[sol]/[glm]/[fable], read-only, HEAD-pinned): blind-solve;
  mutation sweep (categories above); per-technique TAUGHT checklist (U06: sort-key choice / sweep /
  why-it-works / greedy-can-fail; U07: state model / step order / boundary+termination); technique
  metadata + prematurity (greedy/simulation introduced not premature; NO `prefix-sum`/recursion/`deque`/
  converging-two-pointer; NO `.remove`; no untaught builtins/methods); solution-shape matches Big-O;
  manifest==map; structure. Resolve every `[OPEN]`; re-verify in round-2 before consensus.
- [ ] **C5 — commit any content-gate fixes, write the post-execution report** in this file, and re-run full
  `ci-local.sh` ALL GREEN after the fixes — the fix commit, report, and green final CI all precede the PR.
- [ ] **C6 — PR** → `pre-merge-guard.sh --pr` OK → squash-merge → delete branch → update memory.

## Post-Execution Report

_(filled at Phase C)_

## Plan Review

### Round 1 (2026-09-08, HEAD f36949c) — [self] APPROVE · [fable]/[glm] APPROVE-WITH-NITS · [sol] REJECT
All three external reviewers independently VERIFIED the closure boundary correct against `tools/
concept_scan.py` (the `.remove` silent-credit, the untaught-builtin Name-call blindness, and the
premature-technique/feature list — `prefix-sum`(U08)/two-pointer(U14) reviewer-caught, `recursion`/`deque`/
`comprehension`/`bitwise-ops` scanner-caught features; ≤U05 techniques allowed). One blocker + convergent
nits folded (no architecture change):
- **[FIXED] BLOCKER (sol; fable+glm as nit) — `requires ∩ practices` violation.** A5/B5's practices recipe
  listed `sorted-key`/`tuple` (U06) and `input-parse`/`grid-2d` (U07) as practices, but the map already
  seeds them in `requires`. Rewrote to the plan-020 formula `practices = used − requires − introduces −
  wrapper-artifacts`; those concepts stay in `requires` only. Global Constraint + A5 + B5 updated.
- **[FIXED] nit (glm+sol) — enforcement-split / detect() overreach.** `detect()` catches premature FEATURES
  + untaught methods but is BLIND to `.remove` (silently credited) and Name-call builtins (all/any/
  enumerate/zip/reversed/map/filter); reworded Global Constraint + A6/B6 to enforce those by grep + review,
  not `detect()`; corrected the "prefix-sum-ish feature leak" mislabel (prefix-sum is a technique).
- **[FIXED] nit (fable) — ban-list kind labels.** `recursion`/`deque`/`comprehension` are `kind: feature`
  (scanner-detectable), not techniques; relabeled.
- **[FIXED] nit (fable) — float outputs in the greedy problems.** A2 now mandates integer/fixed-format
  outputs (token-compare asserts are float-fragile) and drops the fractional-knapsack ratio idea.
- **[FIXED] nit (fable) — wrap-around `%` framing.** B2 now keeps `%` to plain arithmetic, away from the
  U11 `modular-arithmetic` technique; event processing is an index-walk (no `.pop`/`deque`).
- **[FIXED] nit (fable+glm) — Files lines / commit-before-rerun.** Files lines note the `requires` trim;
  C5 commits content-gate fixes before the report + final ci-local rerun.
Stale-design note (sol nit 6, non-blocking): design §4 still lists six unit teacher-note headings incl
`## Rubric`; the plan correctly follows the shipped 5-heading tooling (`## Rubric` is project-only). No
reviewer found a closure hole or a missing/placeholder task. Round-2 dispatched at the revised HEAD.

### Rounds 2–4 — CONSENSUS: [self] · [sol] APPROVE, [fable] · [glm] APPROVE-WITH-NITS. Gate CLOSED.
fable + glm APPROVE-WITH-NITS at round 1 (their nits folded, verdicts stand). sol's round-1 blocker
(requires/practices) was fixed to the plan-020 subtraction formula; sol confirmed it RESOLVED at round 3
(and agreed `input-parse` → U06 practices matches shipped U02–U05). sol's residual A2 output-format nit
(forbid every fractional string, not just floats/ratios) was tightened and **[sol] APPROVE** at round 4
(HEAD 77e13f3). No open blocker. **Plan-review gate CLOSED — cleared for implementation (Phase A).**

## Content Review

_(4-way content-review gate — findings `[OPEN]`/`[FIXED]`/`[WONTFIX]`, all resolve before merge)_
