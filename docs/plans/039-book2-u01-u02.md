# Plan 039 — Book 2 stdin-first rollout: U01 Reading-the-Input + U02 Boolean Logic

**Goal:** Migrate U01 (Reading the Input) and U02 (Boolean Logic & Algebra) to the stdin-first,
subprocess-judged model (plans 036/038) with worked-example ladders. First rollout batch after the two
pilots (U08, U06). U01 is special: under stdin-first it is the unit that **teaches reading real
stdin** — its lesson gets a genuine reframe, not just a mechanical transform.

**Architecture:** The settled plan-036 contract (stdin `.py` solvers in `assets/`, judged by
`judge-check` against `.in`/`.out` fixtures; `solutions.ipynb` a no-exec display mirroring each `.py`;
lessons = executable literal-data ladder rungs → full stdin solver "Put it together"). No tooling
changes. Per-entry `assets/` switch flips U01/U02; all other entries unchanged.

**Tech stack:** `tools/judge.py` / `tools/source_policy.py`, Jupyter notebooks, `.in`/`.out` fixtures,
`scripts/ci-local.sh`.

## Governing principle & the two-solver rule

**User (2026-09-10): enough worked examples for genuine mastery — NO learning-duration constraint.**
`judge-check` derives expected lesson PIDs as `l1..l{manifest.lessons}` ∪ the lesson's `assets/lN.py`
references; both units are `lessons: 2`, so each ships **TWO** worked lesson solvers (l1, l2) — one per
lesson's "Put it together". Where a unit currently has only one worked `solve()`, author a second from
the unit's own material (more worked examples = better mastery, aligned with the principle). `lessons`
stays 2 for both (advisory; coverage-map unchanged).

## U01 — Reading the Input (introduces `input-parse, str-split, grid-2d`)

The lesson REFRAMES around reading real stdin. Open project-first (contest hands you text; print exact
text). Teach that the real program starts `import sys` / `data = sys.stdin.read()`, then parses.

- **L1 — read values from stdin.** Executable literal-data rungs (each a `**Notice:**`): `.split()` a
  literal `data` string into tokens → `int(token)` → "read N then N values" with a position loop →
  build exact output text (space-join via a loop). Then **Put it together** `l1.py` = the sum-&-maximum
  stdin solver (`data = sys.stdin.read()`; parse N + values; `print(f"{total} {biggest}")`), shown
  `no-exec` + run line + complexity `O(n)`.
- **L2 — read a grid from stdin.** THREE one-increment executable rungs (sol blocker — do not bundle):
  (1) parse the `R C` header into two ints; (2) read ONE row of `C` ints with a token-position counter
  (single loop) and print it; (3) loop `R` rows into a list-of-lists (nested loops, `grid-2d`) and index
  one cell `grid[r][c]`. Then **Put it together** `l2.py` = a grid row-sums stdin solver (grid row-sums
  shape; **use sample data DIFFERENT from exercise 5** so ex5 isn't answered verbatim — fable nit),
  `no-exec` + run line + complexity `O(R*C)`. Completeness: the existing decisive-last / border edge
  notes.
- **Closure:** union introduces input-parse/str-split/grid-2d; requires def/parameters/return/for/
  list/int/type-conversion/print; allowed builtins incl. `sum`/`max`/`len`/`range`/`int`/`str`/`print`/
  `input`. `input-parse`/`grid-2d` are techniques (reviewer); `.split` is str-split. No banned
  construct.

## U02 — Boolean Logic & Algebra (introduces `boolean-algebra, code-tracing`)

Keep the executable truth-table / De-Morgan / short-circuit / simplification demos as ladder rungs
(they already print + assert the equivalences — that verification IS the completeness "why", the
boolean analog of a counterexample). Keep the two code-tracing warm-ups.

- **L1 — boolean algebra.** Rungs (≥3): truth table for `and`/`or`; `not` + precedence + **code-tracing
  warm-up 1** (stays in L1 — fable nit); De Morgan (print + assert both laws agree every row);
  short-circuit; simplify `not(not(member or guest) or banned)` → `(member or guest) and not banned`
  (assert equivalence every row). **Put it together** `l1.py` = a gate-access stdin solver
  (`member guest banned` → `GRANTED`/`DENIED` via the simplified form), `no-exec` + run line + complexity.
- **L2 — code-tracing & deciding.** THREE one-increment rungs before the solver (sol blocker):
  (1) **code-tracing warm-up 2** (the gate checker, predict-then-run); (2) count yes-answers over a
  literal list with an accumulator + print the count; (3) GENERALIZE — detect a dissenter (`found_no`)
  and combine the two facts with `and` (`yes_count*2 > n and found_no`) on literal data, printing the
  decision. Then **Put it together** `l2.py` = the majority-with-a-dissenter stdin solver, `no-exec` +
  run line + a note on the strict-majority `*2` idiom. **`.lower()`: state in the l2 problem that
  answers may be mixed-case (`Yes`/`NO`) and add a mixed-case fixture so `.lower()` is load-bearing
  (else drop it) — glm/fable.**
- **Closure:** union introduces boolean-algebra/code-tracing; requires boolean/logical-ops/comparison/
  if/elif-else/print/variable; allowed builtins; `.lower` is a taught string method (used in the
  majority solver — confirm it's in the practices/allowed set, else avoid it). No banned construct.

## Phases

### Phase A — assets (both units): stdin `.py` solvers + fixtures

For each unit, `assets/`: the lesson solvers `l1`,`l2` (authored per above, transformed to stdin via
the proven recipe — convert only the top-level `return`) + `ex1`–`ex9` from the shipped solutions. Each
solver ≥2 NON-VACUOUS `.in`/`.out` fixtures from the shipped asserts (+ crafted where a lesson solver
needs a 2nd); verify each solver vs fixtures by subprocess. **Per-solver mutation witnesses (name one
killing fixture each — sol nit):** U01 off-by-one-on-N → ex2 (`3 4 / 1 2 9`), ex4 LAST (`3 9 / 1 2 9`);
U01 `l1` decisive-last MAXIMUM → **add a crafted fixture whose max is last** (e.g. `3 / 2 4 9` → `15 9`)
so a max-over-prefix / drop-last bug fails (glm NIT-C); U02 wrong-De-Morgan-flip → ex2 asymmetric rows;
U02 `l2` `.lower()` → **add a mixed-case fixture** (e.g. `Yes`/`NO`) so dropping `.lower()` fails (glm
NIT-B / fable). Confirm each crafted fixture kills its mutant in-session before committing.

### Phase B — lesson + solutions + docs (both units)

Rebuild `lesson.ipynb` to the ladders above (executable rungs + `**Notice:**`; full solvers `no-exec`
mirroring `l1`/`l2.py` + run lines + complexity). **Replace the executable `def solve(data)` lesson
cells** (U01 cell 8 sum&max, U02 cell 16 majority) with the stdin solver cells, AND **delete the old
`import sys; print(solve(sys.stdin.read()))` wrapper cell** — no `def solve` or wrapper survives in any
lesson (glm NIT-A / fable). `solutions.ipynb`: no-exec display mirroring each `exN.py` + a per-exercise
one-line `**Notice:**`. `exercises.ipynb`: rewrite the intro off `solve(data)` to stdin/stdout (U01's
intro currently *defines* the solve contract — rewrite it to teach reading stdin).
`teacher-notes.md`: **rewrite every `solve(data)`-contract statement off the retired model** — U01's
"Hook and contract" bullet ("takes the whole input as one string… returns text, does not print") and
U02's "print from inside `solve`" prose, NOT just `## Pacing`; then re-sync `## Pacing` to the ladders
(5 unit headings, stays 2 lessons; warm-up 1 in L1, warm-up 2 in L2). Also update
`docs/designs/001-book2-algorithms.md §7`'s U01 line off the pre-migration `solve(data)` parenthetical
(glm NIT-D). (U01 `requires: def/parameters/return` may go unused once solvers are top-level scripts —
mechanically fine, `concept-scan` checks used ⊆ union; leave manifests unchanged this batch — fable.)

### Phase C — Verification (named verification phase)

`scripts/ci-local.sh` ALL GREEN: `judge-check` (all solvers l1/l2 + ex1-9 per unit pass ≥2 fixtures;
l1/l2 expected-present; mirrors match), `source-policy` (clean; no lambda/banned), `exec-lessons`
(ladder rungs run; solver cells `no-exec`), `concept-scan`/coverage/prereq/manifest/structure/hygiene/
cell-lint/noexec/stretch, Book-1 PDF, pre-merge-guard. Un-migrated entries stay GREEN on the old path.
**Content audit:** solvers correct + fixtures non-vacuous (each named mutant killed: off-by-one-N,
wrong De-Morgan flip, decisive-last MAX, `.lower()` mixed-case); **each introduced technique has ≥3
one-increment rungs before "Put it together"** (U01 L1 values = 4 rungs, L2 grid = 3 rungs; U02 L1
algebra ≥3, L2 = 3 rungs), each with a Notice; U01 genuinely teaches reading stdin; U02 truth-table/
De-Morgan equivalences asserted every row; both lessons project-first; **NO `def solve`/`sys.stdin`
wrapper cell survives in any lesson, and NO `solve(data)`-contract prose survives in either
teacher-notes or exercises intro**; `## Pacing` == 2 lessons and matches warm-up placement; `l2`
sample ≠ ex5 verbatim.

## Rollout note

Next batch (after this): **U03 Complexity + U04 Sets/Tuples/Sorting + U05 Searching** (rest of Term 1),
to keep the student-facing arc consistent front-to-back (fable nit). Then U07; U09–U12; U13–U14; the
4 checkpoints; the capstone.

## Out of scope

- All other units/checkpoints/capstone (later rollout batches). Tooling. Governance files.
- **Verification-phase note:** ships reworked units WITH a named verification phase (Phase C).

## Post-Execution Report

_(filled at Phase C)_

## Plan Review

### Round 1 (HEAD bbd7190) — [self] APPROVE

- **Closure ✓** — U01 solvers use `.split`/`int`/`sum`/`max`/`range`/`for`/`list-append`/`f-string`/
  `str` (grid via nested loops) — all in the union + allowed builtins; U02 uses `and`/`or`/`not`/
  comparison/`if`-`elif`/`.lower`/`int(x)==1`/`for`/accumulator — `.lower` is `string-methods` (U02
  practices; current U02 passes concept-scan on main). No banned scanner-blind construct.
- **Two-solver rule ✓** — judge expects `l1`,`l2` (manifest floor); authoring a 2nd worked solver per
  unit from its own material (U01 grid row-sums = exercise-5 shape; U02 gate-access = the lesson's
  simplification example / exercise-1) adds a worked example, serving the mastery principle.
- **U01 reframe ✓** — teaching `data = sys.stdin.read()` with parsing rungs on literal data is the
  right pedagogy; the exercises intro (which currently *defines* the solve contract) must be rewritten.
- **U02 completeness ✓** — the truth-table/De-Morgan equivalence assertions (both forms agree every
  row) are the paradigm-free "why"; plus code-tracing. Named verification Phase C present.
- **Fixtures ✓** — from the shipped mutation-hardened asserts (U01/U02 already carry decisive-last /
  off-by-one-N killers + the asymmetric-boolean cases per the content-gate history).

### Round 1 (HEAD bbd7190) — [glm] AWN · [fable] AWN · [sol] REJECT

All three verified closure (`.lower`/`.split`/stdin resolve; no banned construct), the two-solver rule
(l1+l2 per unit from unit material, fail-closed), the U01 stdin reframe, U02 completeness via
truth-table/De-Morgan equivalence, and the named Phase C. One blocker + nits, all `[FIXED]`:

1. `[FIXED]` **[sol blocker] L2 ladders under the graduated-rung floor** — U01 L2 grid bundled too much
   and U02 L2 jumped from warm-ups to the solver. → U01 L2 now THREE one-increment rungs (header → one
   row → list-of-lists+index); U02 L2 now THREE (warm-up 2 → count-yes → generalize to
   `yes*2>n and found_no`) before "Put it together"; Phase C audits ≥3 rungs/technique.
2. `[FIXED]` **[glm NIT-A / fable Nit4] solve(data) contract survives in teacher-notes + executable
   lesson cells** — → Phase B rewrites both teacher-notes off the contract, replaces the executable
   `def solve` lesson cells (not just the wrapper), updates design-001 §7; Phase C audits "no
   def-solve/contract prose survives".
3. `[FIXED]` **[glm NIT-B / fable Nit1] U02 l2 `.lower()` vacuous** — state case-insensitivity + add a
   mixed-case fixture so `.lower()` is load-bearing.
4. `[FIXED]` **[glm NIT-C] U01 l1 decisive-last MAX** — add a max-is-last fixture.
5. `[FIXED]` **[glm NIT-D] design-001 §7 U01 line** updated in Phase B.
6. `[FIXED]` **[fable Nit2] l2 ≡ ex5 verbatim** — vary l2's sample data.
7. `[FIXED]` **[fable Nit3] warm-up 1 placement** — kept in L1; pacing synced.
8. `[FIXED]` **[sol nit] mutant→fixture map** added to Phase A. **[fable Nit5]** next-batch sequencing
   (U03-U05) recorded. **[fable Nit6]** manifests left unchanged (mechanically fine).

### Round 2 (HEAD e95ebfa) — re-dispatched to [sol]/[glm]/[fable]

_(awaiting round 2)_

## Content Review

_(4-way content-review gate — consensus before PR)_
