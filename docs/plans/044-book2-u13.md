# Plan 044 — Book 2 stdin-first rollout: U13 Grids, Graphs & Traversal

**Goal:** Migrate U13 (Grids, Graphs & Traversal) to the stdin-first, subprocess-judged model with
worked-example ladders. Sixth rollout batch — the LAST unit (U01–U12, U14 merged; only checkpoints +
capstone remain after this). U13 is split solo (its own plan) because it is the most complex unit:
3 lessons spanning graph-repr, flood-fill, BFS, and DFS, mixing recursion (with a `visited` set) and a
`deque`. The unit keeps its existing, gate-proven solver LOGIC (closure-clean on `main`); the work is
the `solve(data)`→stdin transform, `.in`/`.out` fixtures from the shipped asserts, and laddered lessons.

**Architecture:** The settled plan-036 contract — stdin `.py` in `assets/`, judged by `judge-check`
against `.in`/`.out`; `solutions.ipynb` a no-exec display **byte-identical** to each `.py`; lessons =
executable literal-data ladder rungs (each a `**Notice:**`) → full stdin solver + run line + complexity.
No tooling changes. Per-entry `assets/` switch flips U13.

**Tech stack:** `tools/judge.py` / `tools/source_policy.py`, Jupyter notebooks, `.in`/`.out` fixtures,
`scripts/ci-local.sh`. **Spec:** `docs/designs/001-book2-algorithms.md` §3/§4/§7.

## Governing principle & the lesson-solver rule

**User: enough worked examples for genuine mastery — NO duration constraint. KEEP AUTO-ROLLING.**
`judge-check` expects `l1..l{manifest.lessons}` ∪ referenced `assets/lN.py`: **U13 → `l1`,`l2`,`l3`**
(lessons:3, 9 exercises). `l1` = the adjacency-list solver (lesson cell 4 — carries `graph-repr`),
`l2` = the BFS fewest-steps solver (cell 8), `l3` = the DFS reachability solver (cell 10). The grid
4-neighbour demo (cell 2) and the recursive flood-fill demo (cell 6) become fresh literal-data rungs.
`lessons`/coverage-map unchanged.

**Contract-transform recipe (proven, plans 039–043):** drop `def solve`, `dedent`, convert ONLY the
solver's OWN top-level `return X`→`print(X)` (AST-scoped; a NESTED recursive helper's returns are
untouched); prepend `import sys` / `data = sys.stdin.read()` — **import-aware** (keep the
`from collections import deque` before `import sys` for the BFS solver). By solve-OWN top-level returns:
one → mechanical; guard (two mutually-exclusive) → `if/else`; early return inside a loop → the
I/O-preserving found-flag form. **Hand-written flat (explicit literal strings — the U10/U12 approach,
NOT the fragile auto-converter — each verified by subprocess): `l2` (c8 BFS, loop-return + deque),
`l3` (c10 DFS, guard), `ex4`, `ex5` (loop-return), `ex6`, `ex7` (guard).** All other solvers (`l1`,
`ex1`,`ex2`,`ex3`,`ex8`,`ex9`) have a single solve-own return (the recursion/BFS logic lives in nested
helpers or after the loop) → mechanical. A pre-Phase-A solve-own-return count over all 9 `exN` + 3 `lN`
re-confirms this set.

## Closure discipline (preserve what each unit already teaches)

- **U13 Grids/Graphs** introduces techniques `graph-repr, bfs, dfs, flood-fill` (grid-2d from U01;
  requires recursion (U09), deque (U10), set-literal/set-ops (U04)).
  - **Adjacency list = a PLAIN dict** built with `if u not in adj: adj[u] = []` then `.append` — NO
    `defaultdict`, NO class. Undirected edges add BOTH directions.
  - **BFS = a `deque` FIFO**: `append` to enqueue, `popleft` to dequeue, `len(queue) > 0` to test — NO
    bare `.pop`. `visited` is a `set` (`{start}` literal, `.add`, `in`); `distance` is a dict; cells are
    `(r, c)` tuples.
  - **DFS / flood-fill = recursion with the `visited` set PASSED AS AN ARGUMENT** — NEVER
    `nonlocal`/`global`. Grid recursion is 4-neighbour and inputs are capped `R·C ≤ 400` (recursion
    depth safe). Base case guards bounds/`#`/already-visited BEFORE recursing.
- **All units:** pinned source-policy builtins `{len,min,max,sorted,sum,abs,round,input,print,range,
  int,str,set}`; no `.pop/.join/.index/.count/.find`, `+=`/AugAssign, chained compare (note the shipped
  code uses `0 <= nr and nr < rows`, NOT `0 <= nr < rows` — preserve that), `[x]*n`, comprehension,
  ternary `IfExp`, `del`/`global`/`nonlocal`. AST-verify after transform.

## U13 — Grids, Graphs & Traversal (introduces `graph-repr, bfs, dfs, flood-fill`; lessons:3)

Restore the unit's project hook in cell 0 (from `main`).

- **L1 — four neighbours in a grid; an adjacency-list graph.** THREE fresh rungs, each a Notice:
  (1) a grid stored as a list of strings; visit the 4 neighbours of a cell with `dr/dc` offset lists,
  guarding bounds with `0 <= nr and nr < rows`; (2) build an ADJACENCY LIST as a plain dict (undirected
  edges add both ways); (3) read a node's neighbours / degree from the dict. Then **Put it together**
  `l1.py` = the adjacency-degree solver (cell 4), `no-exec` + run line + complexity `O(E)`.
- **L2 — recursive flood-fill & breadth-first search.** THREE fresh rungs: (1) recursive FLOOD-FILL of
  a region with a `visited` set passed down, base-casing bounds/`#`/visited; (2) a `deque` FIFO — enqueue
  with `append`, dequeue with `popleft`; (3) BFS layer-by-layer recording a distance dict. Then **Put it
  together** `l2.py` = the BFS fewest-steps solver (cell 8, found-flag flat transform, keeps the deque
  import), `no-exec` + run line + complexity `O(R·C)`.
- **L3 — depth-first search reachability.** THREE fresh rungs: (1) a recursive DFS marking `visited`
  (passed as an argument); (2) follow adjacency-list neighbours, skipping visited; (3) short-circuit
  when the target is found. Then **Put it together** `l3.py` = the DFS reachability solver (cell 10,
  guard→if/else, nested `reaches` preserved), `no-exec` + run line + complexity `O(V + E)`.
- **Closure:** introduces graph-repr/bfs/dfs/flood-fill (confirm each USED); plain-dict adjacency,
  deque FIFO (no bare `.pop`), recursion with `visited` as an argument (no `nonlocal`); **RECURSION /
  flood-fill grids capped `R·C ≤ 400`** — the ITERATIVE BFS exercises (ex4/ex5) legitimately allow
  `R·C ≤ 200000` and ex9 `N,M ≤ 200000` (no recursion-depth risk), so the 400 cap is a recursion
  constraint only.

## Phases

### Phase A — assets: stdin `.py` solvers + fixtures

Build `assets/`: pinned lesson solvers (`l1`=cell 4, `l2`=cell 8, `l3`=cell 10) + `ex1`–`ex9`.
Single solve-return solvers transform mechanically (recursion/BFS in nested helpers or post-loop);
the hand-write set (`l2`,`l3`,`ex4`,`ex5`,`ex6`,`ex7`) is written as explicit FLAT literal strings
(found-flag / if-else), each verified by subprocess to reproduce the shipped assert. Each solver ships
**≥2 NON-VACUOUS** `.in`/`.out` fixtures; **every `.out` computed from the pristine solver** (exact,
one-per-line); dedupe inputs; drop any empty-stdout fixture (judge rejects it); ensure ≥2 remain.
**Named witnesses:**
- **`l1`** (degree): a node whose degree counts BOTH ends of each undirected edge (a directed/one-way
  add mutant undercounts).
- **`l2`** (BFS fewest steps): a genuine FIFO witness — a grid where FIFO BFS reports the TRUE shortest
  distance but a LIFO order (replace `popleft`→`pop`) reports a LARGER one; confirm by actually running
  the mutant that the fixture's answer changes. PREFER a maze where a wall bends the shortest path off
  the straight line so the divergence is pedagogically clear (not merely an enqueue-order artifact).
- **`l3`** (DFS reachability): a YES case reachable only via a MULTI-HOP path (no direct edge; kills a
  "check direct neighbours only" mutant) + a disconnected NO case.
- **The shipped `l2` asserts are FIFO-VACUOUS** (a `popleft`→`pop` LIFO mutant gives the same answer),
  so the constructed FIFO witness above is the ONLY thing making `l2` non-vacuous for the FIFO property
  — do NOT rely on the carried-over asserts. A concrete witness: `4 4 / T.S. / .#.. / .... / .###`
  (FIFO distance 2 vs LIFO 8).
- AST-verify no `.pop`/`nonlocal`/class/comprehension/chained-compare crept in; confirm RECURSION/
  flood-fill fixtures ≤ 400 cells (BFS/iterative fixtures may be larger but stay small for CI speed).

### Phase B — lesson + solutions + docs

Rebuild `lesson.ipynb` to the ladders (fresh executable rungs + `**Notice:**`; full solvers `no-exec`
**byte-identical** via `mirror()` + run line + complexity). **RESTORE the project-first hook in cell 0
from `main`.** Replace every executable `def solve` lesson cell with a stdin-solver cell or a fresh
literal-data rung; DELETE the old wrapper cell — no `def solve`/wrapper survives. `solutions.ipynb`:
no-exec byte-identical mirrors + a per-exercise `**Notice:**` **derived from each exercise's ACTUAL
solver** (READ each one — a wrong Notice is a REJECT; label BFS vs DFS vs flood-fill correctly).
`exercises.ipynb`: rewrite the intro off `solve(data)` (keep the "no `solve()` wrapper" line) +
CASE-INSENSITIVE whole-word `return`/`returns`/`returned`→`print`/`prints`/`printed` sweep of STATEMENT
cells, **then REPAIR any legitimate FUNCTION-return prose the sweep corrupts** (a recursive base case
"return true/0", a helper's result — batch-4/5 lesson). `teacher-notes.md`: reword every `solve`/
`return`(/`returns`/`returned`) token off the retired model, then re-sync `## Pacing` to the rebuilt
ladders (5 section headings; lesson count 3). **Also correct two pre-existing teacher-notes prose
errors (sol/glm): the Ex 7 line ("Separate Networks") says "DFS connectivity (is every node in one
network?)" but the shipped solver does DFS REACHABILITY (`YES` iff `target` is reachable from `start`)
— fix ALL THREE Ex-7 "connectivity" mentions in teacher-notes (the pacing line, the Big-O line, and any
Goals/prompt reference); and the FIFO-vs-LIFO witness note should name the new constructed `l2` witness,
not only ex4/ex9.** (Import-aware transform note applies to `ex9` too, which also carries
`from collections import deque` — keep that import in its stdin form.) **Backstop greps (zero): whole-word case-insensitive
`return`; whole-word `solve`; `solve(` — over exercises statements + teacher-notes** (except the intro
"no `solve()` wrapper"). **Design §7 verify-only** (U13 line carries no `solve(data)` parenthetical).

### Phase C — Verification (named verification phase)

`scripts/ci-local.sh` ALL GREEN: `judge-check` (`l1,l2,l3`+`ex1-9`; ≥2 fixtures; expected `lN`; mirror
match — byte-identity verified separately), `source-policy` (no bare `.pop`/lambda/banned/`nonlocal`),
`concept-scan` (recursion + deque legal; no untaught construct — note the four TECHNIQUES are in
concept-scan's `never_flag`, so their being USED is enforced by coverage/introduction checks + the
manual audit, not concept-scan),
`exec-lessons` (every rung runs fast under the 120 s cap — grids small; solver cells `no-exec`),
coverage/prereq/manifest/structure/hygiene/cell-lint/noexec/stretch both books, Book-1 PDF,
pre-merge-guard. **Content audit:** every solver correct + fixtures non-vacuous (each named mutant
killed — esp. the BFS FIFO witness: `popleft`→`pop` changes `l2`'s answer); ≥3 one-increment rungs per
technique-lesson each with a Notice; rungs fresh + distinct from `lN` mirrors; lesson project-first
(hook restored); adjacency is a plain dict, BFS uses deque (no bare `.pop`), DFS/flood-fill recursion
passes `visited` as an argument (no `nonlocal`); recursion/flood-fill fixtures ≤ 400 cells; NO `def solve`/wrapper in any lesson; NO
`solve(data)` prose/bare `solve`/any `return` in exercises statements or teacher-notes; each solutions
Notice names the right technique; `## Pacing` matches 3 lessons; no lesson solver sample reproduces an
exercise sample verbatim; mirrors byte-identical.

## Rollout note

Next: the **4 checkpoints (CP1–CP4)**, then the **project-03 capstone** — the last entries. Checkpoints
use `## Question N` (6–8), teacher-notes add `## Grading`; the capstone uses `## Milestone N` + `##
Make it yours` + `## Requirements` and adds `## Rubric`. Each a plan through both 4-way gates to merge.
On the capstone's merge, **Book 2 is fully migrated to stdin-first.**

## Out of scope

- Checkpoints and the capstone (later batches). Tooling changes. Governance files.
- Changing any shipped solver's ALGORITHM or the unit's manifest concepts (presentation + contract).
- **Verification-phase note:** ships a reworked unit WITH a named verification phase (Phase C).

## Plan Review

### Round 1 (HEAD cf9476b) — [self] APPROVE

- **Designations + hand-write set verified** — solve-OWN top-level return counts: `l1`=c4 (1,
  mechanical), `l2`=c8 (2, loop-return → hand-write; BFS logic + deque), `l3`=c10 (2, guard → hand-write;
  nested `reaches` preserved). Multi/loop-return `exN` = ex4,ex5 (loop), ex6,ex7 (guard). Full set =
  {`l2`,`l3`,`ex4`,`ex5`,`ex6`,`ex7`}; all else mechanical (recursion/BFS in nested helpers/post-loop).
- **Closure** — adjacency = plain dict (`if u not in adj`), BFS = deque FIFO (no bare `.pop`),
  DFS/flood-fill recursion passes `visited` as an argument (no `nonlocal`), grids ≤ 400, bounds guarded
  with `0 <= nr and nr < rows` (not chained). Preserved by the transform.
- **Witnesses / rungs / Phase C ✓** — ≥3 fresh rungs per lesson; the `l2` BFS FIFO witness
  (`popleft`→`pop` changes the distance) is the key non-vacuity check; Phase C explicit. Project-first
  hook to be RESTORED from `main`. Scope: U13 solo — the right call for the most complex unit.

### Round 1 (HEAD cf9476b) — [glm] APPROVE WITH NITS · [fable] APPROVE WITH NITS · [sol] REJECT

All three verified closure (plain-dict adjacency, deque FIFO no bare `.pop`, recursion with `visited`
as an argument no `nonlocal`, non-chained bounds), the hand-write set (solve-own return counts = exactly
{`l2`,`l3`,`ex4`,`ex5`,`ex6`,`ex7`}), rung floors, Phase C, and — importantly — that the shipped `l2`
BFS asserts are FIFO-VACUOUS (so a constructed LIFO witness is mandatory). [sol] REJECTed on 2 items
(also glm/fable nits); all `[FIXED]` at the HEAD below (plan text only):

1. `[FIXED]` **[sol-1 / glm-N1 / fable-N1] "grids ≤ 400" was internally inconsistent** — the 400 cap is
   a RECURSION/flood-fill constraint; the iterative BFS exercises (ex4/ex5) allow `R·C ≤ 200000` and ex9
   `N,M ≤ 200000`. Qualified the closure, Phase A, and Phase C wording accordingly.
2. `[FIXED]` **[sol-2] Phase B must correct a teacher-notes prose error** — the Ex 7 ("Separate
   Networks") line says "DFS connectivity (is every node in one network?)" but the shipped solver does
   DFS REACHABILITY (`target` from `start`). Phase B now mandates the fix (+ the FIFO-witness prose
   refresh, glm-N4).
3. `[FIXED]` **[glm-N2] concept-scan attribution** — the four techniques are in `never_flag`; their
   being USED is enforced by coverage/introduction + the manual audit, not concept-scan.
4. `[FIXED]` **[fable-N2] import-aware note** — `ex9` also carries the deque import; noted it applies
   beyond `l2`.
5. `[FIXED]` **[fable-N3 / sol] `l2` FIFO witness** — recorded that the shipped `l2` asserts are
   FIFO-vacuous and pinned a concrete witness (`4 4 / T.S. / .#.. / .... / .###` → FIFO 2 vs LIFO 8).

sol/glm/fable confirmed the shipped solvers correct + the designations/hand-write set exact; only plan
text changed.

### Round 2 (HEAD 5bf4fbb) — [sol] APPROVE · [glm] APPROVE WITH NITS · [fable] APPROVE → CONSENSUS

All four APPROVE / APPROVE WITH NITS; no open blockers — plan-review gate CLOSED. Each external
re-verified the 400-cap qualification against the shipped constraints (recursion ex2/3/6/7/8 ≤ 400;
BFS ex4/5/9 ≤ 200000), the Phase-B teacher-notes ex7 reachability correction, the concept-scan
`never_flag` attribution, ex9's deque import, and the FIFO witness (sol + glm + fable each hand-traced
or ran it: FIFO 2 vs LIFO 8, shipped asserts FIFO-vacuous). Two glm nits `[FIXED]` at the HEAD below:
the `l2` witness wording no longer overclaims "wall forces off Manhattan" (prefers a bending maze,
verify by running the mutant); Phase B now says to fix ALL THREE Ex-7 "connectivity" mentions in
teacher-notes, not just one. **Plan-review gate CLOSED — cleared for implementation (Phases A–C).**

## Post-Execution Report

**Status:** Implemented Phases A–C; `scripts/ci-local.sh` **ALL GREEN** (pre-merge-guard OK). Plan-review
gate closed 4-way. Cleared for content-review gate. **This was the LAST unit — U01–U14 now all migrated.**

- **U13 Grids/Graphs** (`lessons:3`): `assets/` = `l1` (adjacency degree), `l2` (BFS fewest steps),
  `l3` (DFS reachability) + `ex1`–`ex9`. L1 rungs grid-4-neighbours → adjacency dict → degree; L2 rungs
  recursive flood-fill → deque FIFO → BFS distance; L3 rungs DFS-mark → skip-visited → short-circuit
  reach. Plain-dict adjacency (no defaultdict/class), deque FIFO (no bare `.pop`), recursion with
  `visited` passed as an argument (no `nonlocal`). Multi/loop-return `l2`,`l3`,`ex4`,`ex5`,`ex6`,`ex7`
  hand-written FLAT (found-flag BFS / if-else DFS, nested helpers preserved). **`l2` FIFO witness
  verified: FIFO 2 vs LIFO 8 — `popleft`→`pop` changes the answer** (the shipped asserts were
  FIFO-vacuous); `l3` multi-hop YES kills a direct-neighbours-only mutant, disconnected NO. Import-aware
  transform kept the deque import in `l2`/`ex4`/`ex5`/`ex9`. teacher-notes: the Ex 7 "connectivity"
  mislabel corrected to DFS reachability in all 3 mentions.
- **Verification:** judge-check/source-policy/concept-scan PASS; all 12 solvers subprocess-verified
  against ≥2 fixtures (every `.out` from the pristine solver); mirrors byte-identical; recursion inputs
  small; full `ci-local` ALL GREEN. **After U13 merges, only CP1–CP4 + the capstone remain.**

## Content Review

### Round 1 (HEAD 6dd73b9) — [self] APPROVE

- **Correctness + non-vacuous fixtures ✓** — 12 solvers subprocess-verified; witnesses kill their
  mutants (l2 FIFO `popleft`→`pop`: 2→8; l3 multi-hop direct-only: YES→NO).
- **Closure ✓** — judge/source-policy/concept-scan PASS; plain-dict adjacency, deque FIFO (no bare
  `.pop`), recursion with `visited` as an argument (no `nonlocal`), non-chained bounds.
- **Ladders ✓** — ≥3 one-increment rungs per technique-lesson each with a Notice; rungs fresh; all
  execute; lesson project-first (hook restored); NO `def solve`/wrapper survives.
- **Contract sweep ✓** — no `return`/bare `solve` in exercises statements or teacher-notes (ex7
  connectivity mislabel fixed); byte-identical mirrors; ci-local ALL GREEN.

### Round 1 (HEAD c8a25f5) — external reviewers ([sol]/[glm]/[fable])

All three independently confirmed: 44 fixture pairs match oracles + real subprocess runs; the named
mutants are discriminating (l2/1 FIFO 2 vs LIFO 8; l3 multi-hop YES vs direct-only NO; l3/2 disconnected
NO; Ex4/Ex9 also FIFO-killing); all 12 no-exec mirrors byte-identical; AST audit clean (no comprehension /
`+=` / chained-compare / class / `nonlocal` / bare `.pop` / `def solve`); manifest/hygiene/structure/
no-exec/cell-lint/stretch/judge/source-policy/prereq/coverage/concept-scan all PASS.

- **[glm] APPROVE WITH NITS** — (glm-1) teacher-notes L3 pacing still said "whether a graph is one
  connected network"; (glm-2) FIFO-witness note named only Ex4/Ex9, not the constructed `l2` witness.
- **[fable] APPROVE WITH NITS** — (fable-1)=glm-1; (fable-2)=glm-2 + flagged retired "asserts" wording;
  (fable-3) **new**: `l2/{1,2,3}.in` carried vestigial trailing `0 0` tokens (not in the "R C + grid"
  format, student-visible via the run line).
- **[sol] REJECT** → three Must-Fix: (sol-1) L3 rung 2 re-claimed the cycle guard already present in rung
  1 and bundled several changes, so the ladder was not one-increment; (sol-2) Ex3 solution Notice said
  "unvisited open cell" but islands are `#` land (`== "#"`); (sol-3)=glm-1 (unreviewed worktree edit at
  pinned HEAD).

**Fixes applied (HEAD → next commit):**

- `[FIXED]` **glm-1 / fable-1 / sol-3** — teacher-notes L3 pacing now reads "answering 'is `target`
  reachable from `start`?' by walking the graph from the source" (no whole-graph-connectivity phrasing).
- `[FIXED]` **glm-2 / fable-2** — FIFO-witness note now names the `l2` witness (LIFO reports 8 vs correct
  2) alongside Ex4/Ex9, and "asserts" → "`.in`/`.out` fixtures".
- `[FIXED]` **fable-3** — `l2/{1,2,3}.in` rewritten to the ex4 multi-line "R C + grid" layout with the
  `0 0` stripped; outputs unchanged (2/4/2); FIFO witness still 2 vs 8. (Ex2's `4 5 0 0` is a legitimate
  `R C start_r start_c` flood-fill input and was correctly left intact.)
- `[FIXED]` **sol-1** — L3 ladder rebuilt as a genuine one-increment progression: rung 1 walks & counts
  reachable → **rung 2** now answers reachability by `visited` membership on a *disconnected* graph
  (`3 in visited` True, `4 in visited` False) → rung 3 short-circuits with early `return True`. Rung 2
  executes (`True`/`False`); distinct from rung 1, rung 3, and the l3 mirror.
- `[FIXED]` **sol-2** — Ex3 solution Notice now reads "flood-filling each unvisited land cell (`#`); each
  fresh flood-fill from a new land cell is one more island" — matches the `== "#"` implementation and the
  statement's "land cells are `#`". Mirror cell untouched (still byte-identical to `assets/ex3.py`).

Re-verification: rung 2 exec `True`/`False`; both no-exec mirrors byte-identical; ci-local re-run.
Round 2 re-dispatched to all three external reviewers on the fixed HEAD for consensus.

### Round 2 (HEAD 2b0ff55) — CONSENSUS: all four APPROVE ✅

- **[self] APPROVE** — carried from Round 1; all five findings fixed, ci-local ALL GREEN, exit 0.
- **[glm] APPROVE** — both glm nits `[FIXED]`; sol-1/sol-2/fable-3 fixes verified non-regressing; 12/12
  solvers match 44 `.out`; all 12 mirrors byte-identical; AST closure clean; all unit + book checks PASS.
- **[sol] APPROVE** — sol-1/sol-2/sol-3 all `[FIXED]`; 44 fixtures match subprocess + independent
  oracles; named witnesses discriminating (l2/1 2 vs 8, l3/1 YES vs NO, l3/2 NO); mirrors byte-identical;
  AST audit of 33 sources clean. (exec-lessons Jupyter-socket error in the subagent sandbox is an
  environment limitation, `[WONTFIX]` — the authoritative `scripts/ci-local.sh` run passes exec-lessons.)
- **[fable] APPROVE** — all five findings `[FIXED]` (each re-run, not assumed): l2 fixtures 2/4/2 + FIFO
  witness 2 vs 8; L3 ladder genuinely one-increment (rung 2 disconnected-graph membership → `True`/
  `False`); Ex3 Notice = land cell `#`; teacher-notes reachability + `.in`/`.out` wording. 44/44 fixtures,
  12/12 byte-identical mirrors, closure clean, contract sweep clean, manifest unchanged, no regressions.

**Gate CLOSED — 4-way consensus, no `[OPEN]` findings.** Cleared for `pre-merge-guard --pr` → PR →
squash-merge.
